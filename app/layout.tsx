import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";
import Providers from "@/components/Providers";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "ReSight - Gaming Utility for Crosshair Overlays & Display Settings",
  description:
    "ReSight is a gaming utility for crosshair overlays, display settings, Discord volume control, and gaming profiles. Enhance your gaming experience with powerful customization tools.",
  keywords: ["crosshair", "overlay", "gaming", "display", "discord", "profiles"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} min-h-screen flex flex-col`}>
        <Providers>
          <Header />
          <main className="flex-grow">{children}</main>
          <Footer />
        </Providers>
      </body>
    </html>
  );
}
